name: flutter_mqtt_exam
description: A new Flutter project.


publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  ## provider
  provider:
  ## shared preferences
  shared_preferences:
  ## MQTT client
  mqtt_client:


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
